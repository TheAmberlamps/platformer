# Project config, read by usagi at launch and by `usagi export` / `usagi tools`.
# Format is `key = value`, one per line. Blank lines and `#` comments are ignored.
name = Game
game_id = com.usagiengine.YOURGAMENAME
game_height = 176

# Additional config options you can set and their current default values:
# game_width = 320
# sprite_size = 16
# pixel_perfect = false
# initial_fullscreen = false
# gif_length = 5
